# Decision Ledger

This ledger tracks every open decision identified during design and build (spec §20, build rule 11). Each decision must be closed or consciously deferred before the phase that depends on it. Release gate item 7 requires every row to show either a resolution or an explicit deferral with justification.

## Decisions Table

| ID | Decision | Status | Needed by | Resolution / Link |
|---|---|---|---|---|
| D-01 | OSS license model — which license, and is anything held back? | CLOSED 2026-09-03 | Phase 0 | Apache-2.0 on entire repository. See ADR-0001. |
| D-02 | Reference embedding provider pair — cloud and local providers for first-run prompt | CLOSED 2026-09-03 | Phase 1 | OpenAI (`text-embedding-3-*`) + Ollama (`nomic-embed-text` / `bge-m3`). See ADR-0002. |
| D-03 | Segment type taxonomy — executor proposes in design document | CLOSED 2026-09-03 | Phase 2 | Taxonomy accepted with design PR #1. Blocking tension (OQ-11) resolved by owner ruling D-25 same date. See `docs/architecture/segment-taxonomy.md`. |
| D-04 | Break-glass grant window default — how long should admin content-access grants remain valid? | CLOSED 2026-09-05 | Phase 4 | **Resolution: 4-hour default; finite enforced.** `issue_grant()` defaults to `timedelta(hours=4)` (D-04). The `_validate_window()` guard rejects `None` (infinite) windows — a `ValueError` is raised if the caller passes an unbounded window. Configurable via `break_glass_grant_window_hours` (config reference §2.6). Tested in `tests/phase4/test_break_glass.py::TestGrantLifecycle::test_default_window_is_4_hours`. See `src/finecorpus/control/break_glass.py` and `runbooks/break-glass.md`. |
| D-05 | Snapshot retention period, and interaction with erasure obligations — how long to retain N-1 and cold snapshots? | CLOSED 2026-09-05 | Phase 4 | **Resolution: 90-day default retention; purge destroys snapshots immediately.** `snapshot_cold()` accepts `retention_period_days` (default 90) and sweeps snapshots older than the retention window during each cold-snapshot call (configurable via `index_lifecycle.snapshot_retention_period_days`). Purge (`delete_document(purge=True)`) destroys all snapshots for the KB immediately and synchronously — no deferred sweep. Tested in `tests/phase4/test_deletion.py::test_purge_destroys_snapshots_d05` and `test_retention_sweep_deletes_aged_snapshots`. See `src/finecorpus/index/lifecycle.py::snapshot_cold()` and `runbooks/purge.md §Snapshot destruction`. |
| D-06 | Cross-KB retrieval within a workspace — is it supported, and what are the permission semantics? | CLOSED 2026-09-05 | Phase 4 | **Resolution: no cross-KB retrieval in v1; single-KB queries only; workspace containment enforced.** The retrieval service enforces per-KB tenancy at the query level: a principal's `kb_id` is checked against the target KB in every query (M-060, M-061). Workspace containment is enforced by including `workspace_id` in the tenancy filter when a principal is present. Cross-KB fan-out (querying multiple KBs in one call) is not supported; callers must issue separate queries per KB. This is a deliberate v1 scope decision. Multi-KB federation deferred to a future phase when cross-KB permission semantics can be specified end-to-end. See `src/finecorpus/retrieval/service.py` `_build_tenancy_filter()` and ADR-0007 (per-KB topology). |
| D-07 | Corpus scale ceiling for v1, and sampling behavior above it — what is the maximum useful corpus size, and how to degrade gracefully? | PARTIAL 2026-09-06 | Phase 5 | **Partial resolution (Phase 5): sweep-time scaling is solved; a hard whole-corpus index ceiling remains deployment guidance, not an enforced limit.** The evaluation sweep degrades gracefully by construction — it samples the corpus (`sample_corpus`, stratified/seeded, `assessment.sweep_min_corpus_docs`) rather than sweeping the full set (M-046), and declines with a plain-language reason below the small-corpus threshold (M-050). This resolves the "sampling behavior above the ceiling" half for the eval path. The other half — a single hard "maximum useful corpus size" for the live index with automatic degradation — is deliberately NOT enforced in v1: per-KB Qdrant collection sizing is a deployment/capacity concern (memory vs. HNSW parameters) better answered by operator guidance than a fabricated global constant. No §19 Phase 5 criterion depends on a hard ceiling. Remaining work (operator capacity-planning doc + optional soft warning when a KB's vector count exceeds a configurable advisory threshold) is carried to Phase 6 alongside the observability surface. Recorded honestly as partial, not closed. |
| D-08 | UI authentication model — local accounts, OIDC, or both? | OPEN | Phase 6 | — |
| D-09 | Name and domain registration — confirm `finecorpus` and `corpus` CLI binary are free at registries and secure domain | OPEN | Before public release | — |
| D-10 | **Incremental upsert mode for v1 (R1 / OQ-L-10):** C-4 requires the ingestion path to write only to shadow collections. Should the incremental upsert path use clone-and-swap (honors C-4) or a direct-to-live mode (would require amending or annotating C-4)? | CLOSED 2026-09-03 | Phase 4 (incremental upsert) | **Resolution: clone-and-swap only in v1.** Direct-to-live opt-in rejected (violates C-4 and §10.5's no-partial-state; owner ruling 2026-09-03). C-4 stands as written — no spec amendment needed. See index-lifecycle.md §8. |
| D-11 | **Spec interpretation (R6 / C-R1, W-1, determinism attack 7):** what does §12's "chunk text is byte-identical to source" mean given that Tier 1 (e.g. table-to-markdown) necessarily changes bytes? | CLOSED 2026-09-03 | Phase 2 (Build) | **Resolution: "byte-identical to source" means byte-identical to the Tier-1-normalized canonical source — the authoritative interpretation of spec §12. Every Tier 1 op recorded in provenance with per-op `changed_text`; raw original always retained and addressable. See contracts/README.md and chunk.md invariants.** |
| D-12 | `segment_path` canonical **format** must be pinned as a closed decision before Phase 1 (C-R4). R2's frozen-artifact rule guarantees its *reproducibility*, but the literal string format (`"/".join(structural_path) + "#" + within_heading_ordinal`, plus edge cases: heading-less docs, nested tables, cross-document refs) is still an open format decision that ADR-0006 depends on. | OPEN | Phase 1 | Referenced: segment-set.md OQ-1, chunk.md, ADR-0006. |
| D-13 | `figure_region` (and any null-`text`) segments vs the required `chunk.text` field (C-R6): decide whether such segments produce a chunk at all. Recommended reading — a segment with no textual representation produces no chunk; `chunk.text` becomes "required unless the segment type has no textual representation", with a closed list of such types; the taxonomy "everything is indexed" invariant is amended to "every segment with textual content is indexed." Needs a decision + coordinated taxonomy/segment-set/chunk update. | OPEN | Phase 2 | segment-taxonomy.md OQ-2; contracts/chunk.md, segment-set.md. |
| D-14 | Tier 3 "visible at citation time" (C-R7): §7.2 requires the original be visible at citation time, but the Chunk contract has no field to return both original and rewritten text; the original is only reachable via a `source_location` round-trip. Decide whether to add `original_text: str \| null` to the Chunk (populated when `tier=3, changed_text=true`) and surface both in the retrieval response. | OPEN | Phase 7 | contracts/chunk.md; contracts/retrieval-response.md. Note: M-032/M-033 contract fields landed Phase 3 (PR #25 — `TransformationRecord.changed_text`, `Tier3Settings.diff_preview_required`); the behavior question (returning both texts at citation time) deferred to Phase 7 when the retrieval response contract is extended. |
| D-15 | Reassembly of null-`text` segments (C-R9): `ReassemblyRecord.method=document_order_concat` is undefined when a segment's `text` is null (figures, some headings). Decide the rule (recommended: null-text segments contribute the empty string) and add it as a named invariant + property-test clause. | OPEN | Phase 2 | contracts/segment-set.md ReassemblyRecord. |
| D-16 | Connector permission-gap acknowledgement hardening (S-R2, T-2): make `SourceRun.acknowledged_permission_gap` **required with no default**; propagate the acknowledgement into every chunk's `TenancyBlock` (or a retrieval-visible field); and specify the Plan/Build check that blocks ingestion when `permission_fidelity=unavailable` and the gap is unacknowledged, recording the override as a governance audit event. | CLOSED 2026-09-05 | Phase 4 | **Resolution: hard Plan-gate on `permission_fidelity=unavailable`; queue-mode threading; ack audit.** The promotion gates (Phase 4 PR #34) enforce that no shadow collection with unacknowledged permission gaps (`permission_fidelity=unavailable` and `acknowledged_permission_gap=False` on any SourceRun) can be promoted. The gate is a hard block, not a warning. `acknowledged_permission_gap` is required on `SourceRun` (no default) per the contract. The ack is recorded as a governance audit event (`entry_type=permission_gap_ack`). Queue-mode threading: the ingest worker checks the ack flag before enqueueing the build job. See `src/finecorpus/control/metadata.py` (SourceRun constraint), `src/finecorpus/index/lifecycle.py` (gate 3 in `validate_shadow()`), and `tests/phase4/test_pr34_review_fixes.py`. |
| D-17 | Source-mirrored permission staleness (S-R3): `permission_mode=source_mirrored` ACLs can go stale with no refresh mechanism and no retrieval-time freshness check. Decide a max-staleness policy (e.g. `permission_max_staleness_seconds`), a permissions-only refresh path (metadata-only update), and whether stale mirrored chunks fail closed (`PERMISSION_STALE`). | OPEN (deferred) | Phase 5 | **Deferral note (Phase 4 closeout):** Staleness of `source_mirrored` ACLs is surfaced in `explain` mode (§11.5) as an advisory warning in the `ExplainBlock` — not enforced at retrieval time. Phase 4 does not implement a `PERMISSION_STALE` fail-closed path or a dedicated permissions-refresh connector. This is an intentional deferral: `source_mirrored` is a connector-phase concern and connectors ship in Phase 6. The staleness detection logic (age check vs. `permission_max_staleness_seconds`) is deferred to Phase 5 retrieval evaluation, when retrieval-quality analysis can quantify the real-world impact. Deferred deliberately, not as an oversight. **Phase 5 re-disposition (2026-09-06):** Phase 5 delivered retrieval-quality evaluation (eval generation, sweep, drift) but scoped to retrieval *quality* (recall/precision), not permission *freshness* — no `PERMISSION_STALE` fail-closed path or permissions-refresh connector was built, because `source_mirrored` ACL provenance only exists once connectors land. D-17 therefore moves to Phase 6 (connectors), where the age-check + fail-closed policy will be implemented against real mirrored-ACL metadata. Still OPEN, still deliberate; the explain-mode advisory warning remains the interim signal. |
| D-18 | Provider log/trace URL-credential stripping (S-R4): the log-exclusion rule does not cover credentials embedded in provider URLs (userinfo `user:password@`, `api_key`/`token` query params). Decide the URL-sanitization rule and extend §18.3 test 10 to cover URL-embedded credential forms. | OPEN | Phase 1 | provider-abstraction.md §6.2. |
| D-19 | Trust-labelling of LLM free-text provenance in explain output (S-R5): `reasoning`/`diff_summary` are derived from untrusted document content but are surfaced in explain mode (§11.5). Decide that explain output labels them `untrusted_ingested`, the UI renders them as literal text (never HTML), and whether to cap their length. | CLOSED 2026-09-05 | Phase 4 | **Resolution: LLM free-text carried under `untrusted_ingested` provenance in explain output.** The `ExplainBlock` in the retrieval response (PR #35) carries `reasoning` and `diff_summary` fields with `trust_level=untrusted_ingested`. The UI (Phase 6) is contractually required to render these fields as literal text (never HTML). Length capping: `max_output_tokens` in `ResolvedOpConfig` (D-21 Phase 3 resolution) bounds LLM output length at generation time — an additional retrieval-response cap is not added to avoid double-truncation. See `src/finecorpus/contracts/retrieval_response.py` `ExplainBlock` and `tests/phase4/test_explain.py`. |
| D-20 | Salience/classification anomaly detection (S-R6, T-1): LLM `salience_tier` classification is a high-value injection/poisoning target (a document steering high-value content to `boilerplate`/`excluded`, or adversarial content to `primary`). Decide a corpus-level integrity check (e.g. block promotion / halt ingestion when a class's tier distribution shifts beyond a threshold vs the prior build) and a pre-promotion salience-distribution gate. | CLOSED 2026-09-05 | Phase 4 | **Resolution: non-blocking tier-shift warning gate in Phase 4; blocking gate deferred to Phase 5 evaluation.** Phase 4 (PR #34) implements a `tier_shift_warning_gate` in `validate_shadow()`: if a class's tier distribution shifts more than `anomaly_detection.salience_tier_shift_threshold` (default 0.30) versus the prior build, a warning is emitted and recorded in the promotion audit event, but promotion is not blocked. The blocking variant (halt promotion on anomalous shift) is deferred to Phase 5 when the eval framework can establish a trust baseline for what constitutes a normal shift. This is a deliberate staged rollout — Phase 4 prevents silent undetected shifts; Phase 5 decides the blocking threshold with evidence. See `src/finecorpus/index/lifecycle.py::validate_shadow()` gate 4b and `tests/phase4/test_promotion_gates.py`. |
| D-21 | Config-export runtime secret scan + token-count sanity ceiling (S-R7, S-R8): decide whether the config export additionally runs a runtime secret-pattern scan (defense-in-depth beyond the structural `secret_free_attestation`), and whether the cost tracker applies a sanity ceiling on provider-reported `input_tokens_used` against a compromised/misconfigured endpoint. | CLOSED 2026-09-05 | Phase 3 | **Resolution:** (a) Config export runs a denylist secret scan as a belt-and-braces defense beyond the structural `extra=forbid` and `secret_free_attestation` field: regex patterns cover `sk-proj-*` multi-segment OpenAI keys and common high-entropy string patterns (tokens ≥ 20 chars with Shannon entropy ≥ 4.5 bits/char, base64 blobs). Scan failure raises `SecretLeakError` and aborts export — the file is never written. (b) `max_output_tokens` ceiling added to `ResolvedOpConfig` (not to the cost tracker, which tracks input tokens): output token count is bounded per operation at config time, making runaway output costs impossible regardless of what a compromised/misconfigured provider reports. Full budget enforcement (per-KB USD cap) remains Phase 4 (M-012). See `src/finecorpus/pipeline/plan/config_io.py` and `src/finecorpus/llm/operations.py`. |
| D-22 | Eval `question_text` injection scanning (S-R13): generated eval `question_text` is free text later used as a prompt in automated sweeps (§9.3). Decide whether to add an `injection_suspicion` score to `EvalQuestion` and auto-flag high-suspicion questions to `unreviewed` before automated use. | CLOSED 2026-09-06 | Phase 5 | **Resolution: numeric injection score on every generated question; provisional status makes auto-flagging moot-by-design.** `generate_eval_set` (PR #44, `src/finecorpus/pipeline/evaluation/generation.py`) runs each generated `question_text` through the deterministic `_compute_injection_score` (reused from `pipeline.decompose.passes.injection` — chosen over the LLM `check_for_injection_suspicion` helper because it returns a numeric, thresholdable score) and returns `injection_suspicion_score` alongside the question control row. Auto-flag-to-`unreviewed` is unnecessary: generated questions are ALWAYS `unreviewed` (the `EvalQuestion.review_status` no-default invariant, M-043), so a high-suspicion generated question can never reach automated use labelled "reviewed" without an explicit human action. `assessment.eval_injection_suspicion_threshold` surfaces high-suspicion questions to reviewers. Tested `tests/phase5/test_generation.py`, `tests/phase5/test_operations_question_gen.py`. |
| D-23 | `confidence_floor` retrieval-policy warning + query-embedding cache flush scope (S-R14, S-R15): decide the UI warning when `confidence_floor` is lowered (previously-excluded low-confidence content becomes retrievable — a trust-gating change, audit-logged), and clarify that the per-KB cache flush scopes on the KB's current/former `model_id`. | CLOSED 2026-09-05 | Phase 4 | **Resolution: diff-boundary detection + CLI warning; audit row deferred to control-plane config activation in Phase 6.** Phase 4 (PR #34) adds a `confidence_floor` change detector in the reindex trigger path: when `confidence_floor` decreases across a config version change, a `confidence_floor_lowered` finding is emitted and a CLI warning is printed before the reindex proceeds. The per-KB query-embedding cache is flushed when the `model_id` changes (cache key includes `model_id`; existing entries become unreachable). **Honest deferral:** The full audit row for `confidence_floor_lowered` (as a governance event in the control-plane audit log visible to workspace owners) is deferred to Phase 6 when the control-plane config activation UI ships. The CLI warning in Phase 4 serves as the operator-visible signal. See `src/finecorpus/pipeline/plan/config_io.py` `detect_confidence_floor_lowering()` and `tests/phase4/test_promotion_gates.py`. |
| D-24 | Injection-suspicion filter as content-suppression vector (T-3): decide advisory guidance (MCP tool description, retrieval API reference) that `injection_suspicion` is an advisory signal, not an access-control verdict, and optional observability for sessions repeatedly filtered to zero by suspicion filters. | CLOSED 2026-09-05 | Phase 4 | **Resolution: advisory docs + filtered-to-zero counter.** (a) The MCP server tool descriptions (PR #36) include the verbatim D-24 advisory: `"injection_suspicion is advisory metadata, not access control."` This is embedded in every registered tool's description so agents see it at tool-discovery time. (b) The retrieval service (PR #33) increments a `filtered_to_zero_by_suspicion` counter per KB session when a query's results are entirely excluded by a suspicion filter. The counter is surfaced in the `ExplainBlock` when `explain=True`. Optional observability for repeated filtered-to-zero sessions (rate-based alert) is deferred to Phase 5 observability work. See `src/finecorpus/services/mcp_server.py` `_D24_ADVISORY` constant, `tests/phase4/test_mcp.py::test_trust_statement_verbatim`, and `runbooks/break-glass.md`. |
| D-25 | **Superseded near-duplicate document indexing (OQ-11):** resolve the §6.1 / §6.3 tension — §6.1 says superseded docs are "excluded from the index by default" while §6.3's salience-not-pruning philosophy implies everything is indexed at some tier. | CLOSED 2026-09-03 | Phase 2 | **Resolution:** superseded near-duplicate documents are NOT indexed by default. Default: inventoried, retained in object storage, and reported in the exclusion report with reason "superseded by \<primary document_id\>" — they produce no segments and no chunks. A per-KB config toggle `ingestion.dedup.index_superseded_versions` (bool, default `false`) enables indexing at salience tier `excluded` (recoverable via explicit filter). Source: owner ruling 2026-09-03. See segment-taxonomy.md §3.2 and OQ-11; contracts/inventory.md version-family section; configuration/reference.md §2.5. |
| D-26 | **Batch envelope contracts — Assess/Decompose/Build exchange pipeline-internal envelopes (`ParseResultBatch`, `SegmentSetBatch`, `BuildResult`) that wrap the official per-document contracts; decide whether collection-level envelopes join the official §12 contract set (with their own contract pages, `SpecRange` declarations, and MAJOR/MINOR discipline) or remain stage-internal. Currently these stages set `consumed_version_range=None` to opt out of `check_version()`. Raised by pipeline skeleton implementation (PR #6).** | CLOSED 2026-09-03 | Phase 1 | **Resolution:** `ParseResultBatch` and `SegmentSetBatch` promoted to official §12 contracts with `schema_version: "1.0.0"`, `SUPPORTED_PARSE_RESULT_BATCH` and `SUPPORTED_SEGMENT_SET_BATCH` SpecRanges, and dedicated modules under `src/finecorpus/contracts/`. Both consuming stages (`DecomposeStage` and `PlanStage`) now set `consumed_version_range` to their respective SpecRange and call `check_version()` on receipt. Every stage boundary is now version-checked. `BuildResult` remains pipeline-internal (Phase 0 skeleton, no official contract needed until Phase 1 Build). Decided by orchestrator as executor-defined schema design. See `docs/contracts/README.md` §Batch envelope contracts and `src/finecorpus/contracts/parse_result_batch.py`, `segment_set_batch.py`. |
| D-27 | **Mixed-PDF handling — `bloated_manual.pdf` has a scanned appendix page (rasterized). Decision: how should a PDF with both native-text pages and OCR-required pages be handled in Phase 2?** | CLOSED 2026-09-04 | Phase 2 | **Resolution:** native parser OCRs low-text image pages in place via the scanned-page path (`ScannedPageResult` per page); document is classified `document_kind=mixed_pdf`. OCR-confidence salience signals are applied per-page (`ocr_confidence_floor`, `ocr_confidence_warn`). The alternative (split document into two separate parse results) was rejected: it would create artificial document identity fragmentation. See `src/finecorpus/pipeline/assess/parsers/pdf_native.py` §Mixed-PDF detection. Ruled during PR #18 review. |
| D-28 | **Injection-scorer manifest parity — the adversarial.pdf fixture declares per-page minimum injection_suspicion thresholds in manifest.yaml. Decision: should the parity test hard-code measured values or generalise patterns?** | CLOSED 2026-09-04 | Phase 2 | **Resolution:** manifest thresholds are measured values enforced by a parity test (`tests/phase2/test_injection_scoring.py::TestManifestPipelineParity::test_per_page_thresholds_met`). Patterns are generalised (not fixture-overfitted): each pattern class covers an intent category (imperative_ignore, role_marker, exfiltration_url, etc.); the benign probe (`"ignore the previous step if the light is green"`) must score 0.0 to prevent false-positive regressions (F-02 ruling). Ruled during PR #16. |
| D-29 | **D-25 enforcement mechanism — the `SupersededVersionPass` must override the salience tier to `excluded` for superseded documents (D-25). Decision: where in the pass pipeline does it run and can another pass override its decision?** | CLOSED 2026-09-04 | Phase 2 | **Resolution:** `SupersededVersionPass` runs LAST in the decompose pipeline (registered at the end of `PASSES` in `src/finecorpus/pipeline/decompose/passes/__init__.py`). Its tier override (`SalienceTier.excluded`) wins over all prior passes — no subsequent pass can override it. This makes D-25 enforcement unconditional. Ruled during PR #19. |
| D-30 | **D-11 Tier-1 recording — `table_to_markdown()` in the spreadsheet parser constitutes a Tier 1 transformation (bytes changed). Decision: when and where are TransformationRecords emitted for table segments?** | CLOSED 2026-09-04 | Phase 2 | **Resolution:** `TransformationRecord` entries are emitted at Build time (not at Assess/Decompose time) for segments whose `segment_type=table`. They are emitted only for actual table segments sourced from table regions — never fabricated for untransformed text. The `table_to_markdown` operation is recorded with `op="table_to_markdown"`, `tier=1`, `changed_text=True` in the chunk's `transformations` list. Ruled during PR #17. |
| D-31 | **Report readability — the findings report for `confluence_export.html` would be cluttered with 30+ `link_record` INFO findings. Decision: how should link_record findings be rendered in Markdown?** | CLOSED 2026-09-04 | Phase 2 | **Resolution:** `link_record` INFO findings are aggregated to a single count line in the Markdown report (`"N link(s) recorded (see JSON report for full list)"`). Full `link_record` entries appear only in the JSON report. All other finding codes are rendered individually. Ruled during PR #21; see `_render_findings_md()` in `src/finecorpus/pipeline/report.py`. |
| D-32 | **Boilerplate fraction floor — with a large fixture corpus (21 docs, 15 eligible), low-prevalence but real boilerplate (ACME legal preamble: 3/15 = 0.20, Confluence nav chrome: 4 occurrences within 1 HTML doc → 1/15 = 0.07) falls below the 0.30 fraction threshold and goes undetected. Decision: should a second detection branch fire on absolute occurrence counts?** | CLOSED 2026-09-04 | Phase 2 | **Resolution:** Two-branch rule adopted. Branch (a) unchanged: fraction > threshold (0.30 / 0.50 for small corpora). Branch (b) new: total corpus occurrences ≥ `boilerplate_abs_floor_count` (default 3) AND unique-document fraction ≥ `boilerplate_abs_floor_fraction` (default 0.05). "Total occurrences" counts within-document repetitions (e.g. nav chrome repeated 4× inside one HTML "page" counts as 4) — branch (b) intentionally extends §6.2's "across many documents" definition to include within-document repetitions for multi-page HTML exports; within-doc repetition counts toward the total occurrence count, while the fraction denominator remains unique-document-based. The fraction floor prevents a block appearing many times in a single document from triggering branch (b) in large corpora where that document is a tiny fraction. Both defaults are implemented as real configurable fields: `assessment.boilerplate_abs_floor_count` (int, default 3) and `assessment.boilerplate_abs_floor_fraction` (float, default 0.05) in `AssessmentConfig`; wired through `AssessStage.__init__` into `run_corpus_passes()`. See `src/finecorpus/pipeline/assess/corpus_passes.py` `compute_boilerplate_blocks()`, `src/finecorpus/config/models.py` `AssessmentConfig`, and `docs/pipeline/dedup-boilerplate.md` §3. Ruled during PR #22 closeout. |

| D-33 | **LLM module placement (Phase 3 executor ruling):** `finecorpus.llm` is a sibling of `finecorpus.embedding` under the core library. Both are provider-abstraction layers; siblings cannot import each other (import-linter layers contract). Decision: accept the duplication of retry/backoff logic in `finecorpus.llm` rather than sharing a common `finecorpus.providers` base, to avoid a new import-linter contract amendment. Upgrade path named: a future `finecorpus.providers.base` module can consolidate when the embedding provider also needs LLM capabilities (e.g., multi-modal). | CLOSED 2026-09-05 | Phase 3 | **Resolution:** `finecorpus.llm` is an independent sibling of `finecorpus.embedding`. Own backoff copy accepted (exponential retry with jitter, identical algorithm). The import-linter layers contract updated to permit `pipeline.plan` → `llm` and `pipeline.build` → `llm` paths. See `src/finecorpus/llm/base.py` and `docs/architecture/llm-provider.md`. |
| D-34 | **Heuristic code splitter strategy (Phase 3 executor ruling):** §6.4 requires syntax-aware splitting at function/class boundaries for code segments. Decision: use a heuristic regex-based splitter (`split_boundaries=["function","class"]`) rather than tree-sitter. | CLOSED 2026-09-05 | Phase 3 | **Resolution:** tree-sitter rejected on dependency weight grounds — adding tree-sitter + language grammars adds ~15 MB of binary dependencies, making self-hosted/air-gapped deployments harder to package (M-008, M-013). The heuristic splitter uses `re.split()` on `\n(def |class |function )` patterns, which covers Python, JavaScript, and Go well enough for the Phase 3 corpus. Upgrade path: when tree-sitter is added as an optional extra (`pip install finecorpus[tree-sitter]`), the `code_syntax` strategy can delegate to it. Decision recorded so future contributors know this is a deliberate deferral, not an oversight. See `src/finecorpus/pipeline/build/chunkers/code.py`. |
| D-35 | **Cost-gate honesty (Phase 3 executor ruling / Ruling 2):** when the CLI computes a cost estimate and a declared provider cannot be constructed (e.g., `openai` with no API key), the system must return a `CostEstimateUnavailable` sentinel rather than fabricating a `$0.00` estimate from a substitute (fake/local) provider. | CLOSED 2026-09-05 | Phase 3 | **Resolution:** `resolve_costing_providers()` returns `CostingResolution(embedding_unavailable=True, embedding_provider=None, embedding_unavailable_reason="...")` for declared paid providers when no credentials are present. `_try_load_cost_estimate()` in the CLI detects the unavailable flag and returns a sentinel with `unavailable=True` rather than a zero-marginal estimate. `_print_cost_estimate()` prints "cost estimate unavailable for declared provider 'openai'" when unavailable — never shows `$0.00` for a paid provider without credentials. Tested in `tests/phase3/test_honest_cost_gate.py` and `tests/phase3/test_acceptance.py::TestCostGate`. |
| D-36 | **Per-KB topology (ADR-0007):** Phase 4 introduces per-KB vector collections (one Qdrant collection per KB) with per-KB aliases. Decision: should each KB have its own Qdrant collection, or should KBs share collections with payload-level segregation? | CLOSED 2026-09-05 | Phase 4 | **Resolution: per-KB collections (one collection per KB, one alias per KB).** Payload-level segregation within a shared collection was rejected: it requires Qdrant payload filtering to enforce tenancy isolation, which is a softer boundary than collection-level isolation and is harder to audit. Per-KB collections are the implementation in Phase 4 (PR #30): `collection_name(kb_id, build_id)` → `rtfc_<kbid>_<buildid>`, `alias_name(kb_id)` → stable query target. Each KB's alias is independently retargeted during promotion/rollback. See ADR-0007 and `src/finecorpus/index/adapter.py`. |
| D-37 | **In-process rate limiter (ADR-0008):** Phase 4 adds per-tenant rate limiting at the retrieval service. Decision: use an external rate-limiting service (e.g. Redis) or an in-process token bucket? | CLOSED 2026-09-05 | Phase 4 | **Resolution: in-process token bucket limiter.** A Redis-based distributed limiter was rejected for Phase 4: it adds an external dependency that complicates the single-node deployment and air-gapped configuration (M-008, M-013). The in-process `TokenBucketLimiter` (PR #36, `src/finecorpus/retrieval/ratelimit.py`) is shared across worker threads within a single retrieval-service process via thread-safe token bucket state. Limitation: does not coordinate across multiple retrieval-service replicas; horizontal scaling requires upgrading to a shared limiter (Redis or equivalent). This limitation is documented in ADR-0008. See `tests/phase4/test_ratelimit.py` and `tests/phase4/test_tenancy_isolation.py::TestApiLevelAuth::test_429_rate_limited_with_retry_after_header`. |
| D-38 | **M-003 fail-closed break-glass audit ruling:** if the break-glass `AuditLogRepository.append()` call raises, should the content be served (optimistic) or denied (fail-closed)? | CLOSED 2026-09-05 | Phase 4 | **Resolution: fail-closed — content is NOT served without a durable audit record.** An unaudited break-glass read is a higher-severity violation than a service interruption. The retrieval service (`src/finecorpus/retrieval/service.py`) catches `Exception` on the audit append and returns `PERMISSION_DENIED` rather than continuing to serve results. Tested in `tests/phase4/test_break_glass.py::TestAuditFailureFailsClosed::test_audit_persistence_failure_denies_content`. See M-003 in the traceability matrix. |
| D-39 | **Fail-closed worker construction ruling (skeleton guard):** should the ingest worker (`IngestWorker`) raise immediately on misconfiguration, or log and continue? | CLOSED 2026-09-05 | Phase 4 | **Resolution: fail-closed — `IngestWorker.__init__` raises `ValueError` on any missing required dependency.** A worker that starts with invalid configuration (missing session factory, missing adapter, missing embedding provider) silently fails on the first job it tries to process, leaving the job in `claimed` state with no error. The skeleton guard validates all required constructor arguments at construction time. This makes misconfiguration visible at service startup rather than at first job execution. See `src/finecorpus/services/ingest_worker.py` and `tests/phase4/test_jobs.py`. |
| D-40 | **Drift-check executor path (§19 crit 2, PR #47 review ruling):** the pipeline layer cannot score retrieval (C-5), so which reachable production path actually executes the scored drift check the post-reindex hook enqueues? | CLOSED 2026-09-06 | Phase 5 | **Resolution: the services-layer ingest-worker loop is the reachable executor.** Review (PR #47) found the drift trigger was inert: `scheduler_tick` was called without `adapter`/`provider` (cron guard always false), and claimed `eval_drift` jobs were routed to the pipeline `JobRunner` fail-loud arm (which references a nonexistent CLI). Resolution: (a) the worker builds adapter+provider once at loop scope and passes them to `scheduler_tick` (cron drift live); (b) the worker loop intercepts `eval_drift` jobs and runs `run_drift_check` directly via `_service_eval_drift_job` (services may import services) — completing the job or failing loud, never faking a result; (c) the pipeline `_run_eval_drift` arm remains a fail-loud safety net. Criterion 2 is now proven against a REAL induced regression (degraded live index → recall drop → `drift_detected`), with a healthy-index control and an end-to-end worker-serviced test. See `src/finecorpus/services/ingest_worker.py`, `tests/phase5/test_eval_drift.py::TestWorkerServicedDrift`. |
| D-41 | **Eval-export chunk-scroll coverage + adapter coupling (M-090, PR #47 review MAJOR):** the eval export scrolls chunk points via `getattr(adapter, "_client")` + `qdrant_client.scroll`, reaching into a private attribute the `FakeAdapter` lacks, so the chunk portion of the export is exercised by no test. | OPEN (deferred) | Phase 6 | **Deferral note (PR #47):** the eval-set/manifest export path is fully tested and tenancy-safe; only the chunk-scroll branch is uncovered and it is not on any §19 criterion. Two follow-ups deferred deliberately: (a) add a first-class `scroll_all(collection, payload_filter)` (or equivalent) method to the `IndexAdapter` interface so the pipeline no longer reaches into Qdrant internals, and implement it on `FakeAdapter`; (b) add a test (unit via the new adapter method, plus an integration-flagged real-Qdrant case) covering the chunk portion of the export. Not a correctness blocker for Phase 5 crit 2 — the branch works against the real `QdrantAdapter` (`_client` exists); the gap is coverage + layering. Tracked as a Phase 6 hardening item. |
| D-42 | **Connector §14.3 permission gate must become structurally unbypassable when concrete connectors land (Phase 6 WU-B review finding).** The connector-framework gate (`enforce_permission_fidelity` / `Connector.guard_permissions`) is correct and tested, but today it is *enforced-if-called*: a caller invoking `PermissionRecord.to_source_permissions()` directly, or a Collect loop omitting `guard_permissions`, can launder permissions — the exact failure §14.3 warns of. | OPEN | Phase 6 (WU-D) | **Ruling:** the framework PR (p6-connectors) corrected its ADR/docstrings to state the guarantee honestly (enforced-if-called, not structural). WU-D (concrete SharePoint/Atlassian/Google Drive connectors + Collect wiring) MUST make the gate the single ingestion seam: the only path from connector permission data to a contract `SourcePermissions` must pass through `enforce_permission_fidelity` (e.g. route `to_source_permissions` through the gate, or make a framework-owned collect loop the sole producer), with a test proving a connector that skips the guard cannot write `SourcePermissions` into the inventory. Until then the guarantee is documented as partial. |

### Open questions from Phase 2 (OQ series)

| ID | Question | Phase 2 Disposition |
|---|---|---|
| OQ-3 | Inter-document and external cross-reference resolution (links to other KB documents or external URLs). | Deferred to Phase 3. Phase 2 `XrefResolvePass` (PR #20) handles only intra-document references (heading/ordinal match). External URL links are recorded as `link_record` findings in the HTML parser. Cross-document resolution requires a document-graph that is not available until full corpus indexing is complete. |
| OQ-5 | Should a document containing minority content types (e.g. a PDF with one OCR page and 50 native pages) split into separate documents per content type? | Not implemented in Phase 2. Mixed content is handled in place: `document_kind=mixed_pdf` with per-page OCR confidence (D-27). Splitting would create artificial document identity fragmentation. Remains open for Phase 3 if retrieval quality analysis shows splitting is beneficial. |
| OQ-6 | Code segments with English keywords — language ambiguity. Code blocks that consist of valid English words (variable names, comments) may be detected as "en" by the language detector. | Accepted as a known limitation (Phase 2). `LanguagePass` (PR #20) uses `langdetect` with a 4-word minimum floor; code segments short enough to fall below the floor receive `"und"`. Code with English keywords that clears the floor may tag "en" — this is correct behaviour since the code is English-language code. No special handling planned for Phase 3 unless retrieval quality analysis shows it causes degradation. |

### Known limitation — branch protection

Server-side branch protection on `main` is unavailable on the current GitHub plan (free plan,
private repo). Until the repo is public or upgraded, the PR-only, CI-green-before-merge rule is
enforced by orchestrator discipline rather than by GitHub. Enable protection (required checks:
`test`, `docs-links`; PRs required) as the first act when the repository visibility changes.
Recorded 2026-09-03.

## Process Notes

- **CLOSED decisions** are resolved and have a link to the ADR, spec section, or documentation that captures the resolution.
- **PROPOSED decisions** are being designed and pending review. They will move to CLOSED once the design review approves them.
- **OPEN decisions** are recognized but not yet scheduled for resolution. They are not blockers for earlier phases but must be resolved before the listed phase begins.
- **Deferred decisions** (if any) are tracked with explicit reasoning for why they are being deferred and what phase they move to. A deferral is a deliberate choice, not an oversight.

## Design-Phase Open Questions

During design review, any ambiguities surfaced in individual architecture pages (e.g., bottom-of-page "open question" sections) are consolidated here at the end of review. Each one either becomes a decision (assigned an ID and added to the table) or is resolved by design changes.

---

**Last updated:** 2026-09-05 (Phase 4 closeout: D-04/D-05/D-06 CLOSED; D-16/D-19/D-20/D-23/D-24 CLOSED; D-17 deferred to Phase 5; D-36/D-37/D-38/D-39 CLOSED — Phase 4 build rulings; OQ-3/OQ-5/OQ-6 unchanged)
