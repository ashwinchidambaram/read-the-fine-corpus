# MUST Traceability Matrix

This page captures every binding requirement in the Read The Fine Corpus engineering specification (v0.1), mapped to where the design addresses it, which build phase implements it, and which test layer proves it. It is updated at every phase; an unmapped row is an open item and must never be left silent. The columns "Design ref", "Phase", "Test layer", and "Status" are maintained by the orchestrator as the build progresses. The matrix was first fully mapped at design review, 2026-09-03.

| ID | Spec § | Requirement | Design ref | Phase | Test layer | Status |
|---|---|---|---|---|---|---|
| M-001 | 2.3 | Break-glass: granting content access requires a stated reason, captured at grant time. | `contracts/README.md §Audit & break-glass seeds` (`BreakGlassGrant.reason` required, never blank) | 4 | tenancy-isolation | designed |
| M-002 | 2.3 | Break-glass grants are time-bound and MUST expire automatically; default window MUST be finite. | `contracts/README.md §Audit & break-glass seeds` (`expires_at` required and finite); `configuration/reference.md §2.6` (`break_glass_grant_window_hours`, pending D-04) | 4 | tenancy-isolation | open (D-04) |
| M-003 | 2.3 | Every break-glass grant, every expiry, and every content read under a grant MUST be written to an immutable audit log. | `contracts/README.md §Audit & break-glass seeds` (`AuditRecord` with `grant_id`); `architecture/overview.md §Services` (audit log in control-api) | 4 | tenancy-isolation | designed |
| M-004 | 2.3 | Workspace Owner and KB Editors MUST be notified at break-glass grant time, not after. | `runbooks/README.md` (`break-glass.md` runbook, Phase 4) | 4 | tenancy-isolation | designed |
| M-005 | 3.3 | There MUST NOT be two pipelines — mode is a UI-level presentation concern over one execution engine. | `contracts/ingestion-config.md §Invariants` ("One config object, not two pipelines") | 3 | unit | designed |
| M-006 | 4.1 | Retrieval service, ingestion workers, embedding service, control plane, and web UI responsibilities MUST be independently scalable. | `architecture/overview.md §Services` (scaling column per service) | 0 | integration | designed |
| M-007 | 4.1 | These responsibilities MUST NOT be collapsed into one process. | `architecture/overview.md §Services` (five separate processes listed) | 0 | integration | designed |
| C-1 | 4.2 | The retrieval service MUST be stateless; all state lives in the vector DB or the control-plane database. | `architecture/overview.md §How the hard constraints are enforced` (C-1 row); `architecture/index-lifecycle.md §1 Invariants` | 0 | concurrency | designed |
| C-2 | 4.2 | Agents and applications MUST NOT connect to the vector DB directly, ever. | `architecture/overview.md §How the hard constraints are enforced` (C-2 row) | 0 | security | designed |
| C-3 | 4.2 | All queries MUST target an alias, never a collection name. | `architecture/overview.md §How the hard constraints are enforced` (C-3 row); `architecture/index-lifecycle.md §1 Invariants` | 1 | contract | designed |
| C-4 | 4.2 | The ingestion path MUST write to a shadow collection, never to the live one. | `architecture/overview.md §How the hard constraints are enforced` (C-4 row); `architecture/index-lifecycle.md §8` (clone-and-swap only in v1; direct-to-live rejected by owner ruling D-10, 2026-09-03). C-4 stands as written. | 4 | contract | designed |
| C-5 | 4.2 | Business logic MUST live in the core library, not in the CLI or the UI. | `architecture/overview.md §How the hard constraints are enforced` (C-5 row); `architecture/overview.md §Core library decomposition` | 0 | unit | designed |
| M-008 | 4.3 | Fully local deployment (local embedding models, local vector DB, no egress) MUST be a supported configuration, not a degraded one. | `architecture/provider-abstraction.md §1 Design principles`; `architecture/provider-abstraction.md §7.2 Ollama embedding provider` | 0 | provider-parity | designed |
| M-009 | 4.4 | The backend adapter interface MUST expose alias-equivalent atomic swap, payload filtering, and hybrid search as first-class operations. | `architecture/index-lifecycle.md §4 Alias swap`; `architecture/overview.md §How the hard constraints are enforced` | 1 | contract | designed |
| M-010 | 4.4 | Where an adapter cannot support a capability, it MUST declare that explicitly and the platform MUST surface the limitation to the user rather than silently degrading. | `architecture/provider-abstraction.md §1 Design principles`; `architecture/provider-abstraction.md §2.1 Capability declaration` | 1 | contract | designed |
| M-011 | 4.5 | Ingestion throughput baseline MUST NOT regress (executor establishes it in Phase 1). | `architecture/overview.md §Services` (ingest-worker scaling) | 1 | integration | phase 1 |
| M-012 | 4.5 | Degradation above the reference scale MUST be graceful and stated: sampling, queueing, and explicit limits, never silent truncation. | `configuration/reference.md §2.9` (budget caps); `architecture/index-lifecycle.md §7.3` (scheduled reindex budget cap) | 1 | failure-injection | designed |
| M-013 | 4.6 | Air-gapped operation with no egress MUST remain fully supported. | `architecture/provider-abstraction.md §7.2`; `configuration/reference.md §2.1` (`platform.airgap`) | 7 | provider-parity | designed |
| M-014 | 4.6 | Cloud-hosted operation MUST NOT be a bolt-on. | `architecture/provider-abstraction.md §7.4 Provider parity obligation` | 0 | provider-parity | designed |
| M-015 | 4.6 | Upgrading MUST NOT require reindexing unless the embedding model or chunking config changed. | `contracts/ingestion-config.md §config_version derivation` (retrieval-treatment-only changes excluded from config_version) | 3 | unit | designed |
| M-016 | 6.1 | `crawl` without an allowlist MUST be rejected by config validation. | `contracts/inventory.md §Invariants` ("crawl links MUST have been produced under a config with a domain allowlist") | 1 | contract | designed |
| M-017 | 6.2 | Assessment MUST detect and report: native-text vs. scanned PDF, OCR quality with per-page confidence retained (not thresholded away), table structure loss, empty/near-empty extraction, encoding corruption, repeated boilerplate, password-protected/unreadable files, and content classes requiring special handling. | `contracts/parse-result.md §Invariants`; `contracts/parse-result.md` (`QualityScore`, `PageResult.ocr_confidence`, `BoilerplateCandidate`, `EncodingIssue`) | 2 | contract | designed |
| M-018 | 6.2 | Boilerplate detection MUST be automatic and requires no user configuration. | `contracts/parse-result.md §BoilerplateCandidate`; `architecture/segment-taxonomy.md §4.1` (boilerplate detection signal) | 2 | unit | designed |
| M-019 | 6.3 | The bloated-manual problem MUST NOT be solved by discarding content at ingestion; every segment MUST get a salience tier instead. | `architecture/segment-taxonomy.md §3.2 The indexing invariant`; `architecture/segment-taxonomy.md §3.1 Tier definitions` | 2 | property | designed |
| M-020 | 6.4 (table) | Tables MUST NOT have rows split from their headers. | `contracts/ingestion-config.md §ChunkingStrategy` (`atomic_rows`, `repeat_headers_on_split`); `architecture/segment-taxonomy.md §2.1` (table row) | 2 | unit | designed |
| M-021 | 6.4 (table) | Spreadsheets MUST be triaged into one of three kinds (report, database, model) before ingestion. | `contracts/ingestion-config.md §SpreadsheetTriage` | 3 | unit | designed |
| M-022 | 6.4 (table) | Spreadsheet databases MUST NOT be vectorized; they MUST be detected, excluded, and reported with the reason stated. | `contracts/ingestion-config.md §SpreadsheetTriage` (`disposition: exclude_unservable`); `contracts/segment-set.md §ExclusionRecord` (reason `spreadsheet_database`) | 3 | unit | designed |
| M-023 | 6.4 (table) | Spreadsheet triage MUST be visible and overridable: user sees each spreadsheet's classification and can reclassify in Proficient mode. | `contracts/ingestion-config.md §SpreadsheetTriage` (`source: enum{detected, user_override}`) | 6 | integration | designed |
| M-024 | 6.4 (table) | Cross-references MUST either be resolved at ingestion (inline or followable pointer) or recorded explicitly; silent loss is not acceptable. | `contracts/segment-set.md §CrossReference` (`resolution: enum{resolved, unresolved}`, `target_note` required when unresolved) | 2 | contract | designed |
| M-025 | 6.4 | Heuristic recommendations (where no sweep has been run) MUST be labelled as such. | `contracts/ingestion-config.md §RecommendationProvenance` (`basis: enum{heuristic, sweep_backed, …}`) | 3 | unit | designed |
| M-026 | 6.4 | The ingestion config output MUST be exportable, diffable, version-controllable, and re-importable. | `contracts/ingestion-config.md §Invariants` ("Diffable/re-importable: canonical JSON serialization; round-trips exactly") | 3 | unit | designed |
| M-027 | 6.5 | The UI MUST make class descriptions feel low-effort — per class, not per document, with generated draft text the user edits rather than a blank box. | not yet designed | 6 | docs review | phase 6 |
| M-028 | 6.6 | Stage 5 (Build) MUST be resumable after failure without full restart. | `architecture/index-lifecycle.md §3 State machine` (`FAILED_INGESTION → INGESTING` transition); `architecture/index-lifecycle.md §6` (ingestion worker dies row) | 1 | resumability | designed |
| M-029 | 6.6 | Stage 5 MUST report progress, cost accrued, and estimated completion. | `architecture/overview.md §Services` (control-api job orchestration); `configuration/reference.md §2.9 Budgets` | 1 | integration | designed |
| M-030 | 6.6 | Stage 5 MUST validate the resulting index before it becomes eligible for promotion. | `architecture/index-lifecycle.md §5 Pre-promotion validation gates` (all four gates) | 1 | integration | designed |
| M-031 | 7.2 | Transformation tiers MUST be independently controllable. | `contracts/ingestion-config.md §TransformationSettings` (`tier1_enabled`, `tier2_enabled`, `tier3_enabled` all separate booleans) | 3 | unit | designed |
| M-032 | 7.2 | Tier 3: original MUST always be retained, never overwritten. | `contracts/README.md §Transformation record` ("Every Tier 1 op is recorded…the RAW original is always retained"); `contracts/chunk.md §Invariants` | 3 | unit | designed |
| M-033 | 7.2 | Tier 3: diff preview MUST be shown before commit. | `contracts/ingestion-config.md §Tier3Settings` (`diff_preview_required`); `architecture/provider-abstraction.md §4.3 Rewriting` (`diff_summary` output field) | 3 | integration | designed |
| M-034 | 7.2 | Tier 3: rewritten chunks MUST be flagged in provenance and visible at citation time. | `contracts/chunk.md §Invariants` (Tier 3 `changed_text=true` flag in `transformations`); open (D-14) for returning both texts at citation time | 3 | unit | open (D-14) |
| M-035 | 7.2 | Tier 3 MUST be per-class opt-in, never global. | `contracts/ingestion-config.md §TransformationSettings` (`tier3_enabled` per `ClassRule`); `contracts/ingestion-config.md §Tier3Settings` (`opt_in_ack: bool required true`) | 3 | unit | designed |
| M-036 | 7.3 | Internal LLM calls MUST be configurable: provider, model, temperature, and per-operation overrides. | `architecture/provider-abstraction.md §4.2 Configuration`; `configuration/reference.md §2.4 Providers — Internal LLM` | 3 | unit | designed |
| M-037 | 7.3 | Local models MUST be supported for internal LLM operations. | `architecture/provider-abstraction.md §4.2 Configuration` ("Local providers MUST be selectable for every operation") | 3 | provider-parity | designed |
| M-038 | 7.4 | Before committing an ingestion config, the user MUST be able to preview representative output: sample chunks, augmentation fields, provenance, and (for Tier 3) a diff. | `contracts/ingestion-config.md §Tier3Settings` (`diff_preview_required`); `architecture/provider-abstraction.md §4.3 Rewriting` | 3 | integration | designed |
| M-039 | 7.5 | Unservable content MUST be inventoried, excluded, and reported with a reason. | `contracts/segment-set.md §ExclusionRecord` (`reason` enum, `reason_detail`); `contracts/ingestion-config.md §ExclusionDecision` | 3 | unit | designed |
| M-040 | 7.5 | The exclusion report MUST be readable by a non-technical user, grouped by reason, and MUST state what the user could do about each group. | `contracts/ingestion-config.md §ExclusionDecision` (`remediation` field: "What the user could do — including 'nothing, and that is correct'") | 3 | docs review | designed |
| M-041 | 7.6 | Where the configured embedding model does not support a detected language, the platform MUST warn before ingestion rather than embedding it anyway. | `contracts/ingestion-config.md §LanguageSupportDecision` (`decision: enum{proceed, warned_proceed, blocked}`); `architecture/provider-abstraction.md §2.1` (`supported_languages`) | 3 | unit | designed |
| M-042 | 8 | Every chunk MUST carry: source document identity and version, position within source, structural path, ordered transformation list with tier of each, confidence score (inc. OCR), segment type, salience tier, and tenancy/permission fields. | `contracts/README.md §Provenance block`; `contracts/chunk.md §Chunk (root)` | 1 | contract | designed |
| M-043 | 9.2 | Unreviewed eval sets MUST be labelled "provisional" everywhere they appear. | `contracts/eval-set.md §EvalSet` (`confidence_level: enum{provisional, reviewed, production_derived}`); `contracts/eval-set.md §Invariants` | 5 | retrieval-quality | designed |
| M-044 | 9.2 | Reports MUST state the confidence level explicitly and prominently, not in a footnote. | `contracts/eval-set.md §Invariants` ("a `generated` set with any `unreviewed` question is `provisional`; this label appears everywhere…prominently, not in a footnote") | 5 | docs review | designed |
| M-045 | 9.2 | Reviewed and unreviewed baselines MUST be visually distinguished. | `contracts/eval-set.md §Invariants` ("Reviewed and unreviewed baselines are visually distinguishable downstream") | 6 | docs review | designed |
| M-046 | 9.3 | Configuration sweep MUST sample the corpus rather than sweeping the full set. | `configuration/reference.md §2.5` (`assessment.sweep_min_corpus_docs`); `architecture/provider-abstraction.md §2.3` (cost estimation) | 5 | retrieval-quality | designed |
| M-047 | 9.3 | Configuration sweep MUST show estimated compute cost before execution. | `configuration/reference.md §2.9` (`budgets.sweep_confirmation_threshold_usd`); `architecture/provider-abstraction.md §2.2` (`estimate_cost`) | 5 | unit | designed |
| M-048 | 9.3 | Configuration sweep MUST require confirmation above a configurable cost threshold. | `configuration/reference.md §2.9` (`budgets.sweep_confirmation_threshold_usd`) | 5 | unit | designed |
| M-049 | 9.3 | If the reference configuration is ever changed, previously reported baselines MUST be marked as measured against the old reference rather than silently reinterpreted. | `contracts/eval-set.md §NaiveBaselineRef`; `contracts/ingestion-config.md §NaiveBaselineRef` ("if the reference changes, prior scores are marked against the old one") | 5 | unit | designed |
| M-050 | 9.3 | Below the small-corpus threshold, the platform MUST decline to sweep, say why in plain language, and apply the reference configuration. | `configuration/reference.md §2.5` (`assessment.sweep_min_corpus_docs`); `contracts/eval-set.md §Open questions 1` | 5 | unit | designed |
| M-051 | 9.3 | A finding that "the default is already near-optimal" MUST be reported as such; do not manufacture a delta. | `contracts/ingestion-config.md §RecommendationProvenance` (`basis: heuristic` vs `sweep_backed`) | 5 | retrieval-quality | designed |
| M-052 | 10.3 | Reindex trigger: change-detected MUST distinguish content changes from metadata-only changes to avoid needless rebuilds. | `architecture/index-lifecycle.md §7.2 Change-detected` (content-hash vs metadata-only path) | 4 | unit | designed |
| M-053 | 10.3 | Reindex trigger: config-change MUST be implemented (editing chunking strategy, embedding model, or transformation tier invalidates the index). | `architecture/index-lifecycle.md §7.4 Config-change` | 4 | unit | designed |
| M-054 | 10.4 | A shadow collection MUST NOT be promoted until: chunk count is within expected bounds, no class produced zero chunks unexpectedly, the eval baseline has been run, and regression is below threshold. | `architecture/index-lifecycle.md §5 Pre-promotion validation gates` (Gate 1–4) | 1 | integration | designed |
| M-055 | 10.5 | Chunk IDs MUST be deterministic, derived from stable inputs (document identity, segment path, position, config version). | `contracts/chunk.md §Deterministic chunk ID derivation` | 1 | property | designed |
| M-056 | 10.5 | Document updates MUST be replace-by-document: all chunks from the previous version are removed in the same operation that writes the new ones. | `contracts/chunk.md §Invariants` ("Replace-by-document: removal keys on `source_document_id`"); `architecture/index-lifecycle.md §9.1` | 1 | property | designed |
| M-057 | 10.5 | Orphan detection MUST run after every incremental ingestion; non-zero orphans is an alert, not a log line. | `architecture/index-lifecycle.md §8.4 Orphan detection` | 1 | integration | designed |
| M-058 | 10.5 | Config version MUST be part of chunk identity. | `contracts/chunk.md §Deterministic chunk ID derivation` (`config_version` in canonical string); `contracts/ingestion-config.md §config_version derivation` | 1 | unit | designed |
| M-059 | 11.2 | Every retrieval response MUST include full provenance (Section 8) for every returned chunk. | `contracts/retrieval-response.md §RetrievalResult` (`provenance: Provenance`, required); `contracts/retrieval-response.md §Invariants` | 1 | contract | designed |
| M-060 | 11.4 | Tenant filtering MUST be enforced server-side in the retrieval service and MUST NOT be overridable by any client-supplied parameter. | `architecture/overview.md §Tenant isolation mechanism`; `contracts/README.md §Tenancy block` | 4 | tenancy-isolation | designed |
| M-061 | 11.4 | A test MUST exist that attempts cross-tenant retrieval and asserts failure. | `architecture/overview.md §Tenant isolation mechanism` (Test §18.3 test 2 described) | 4 | tenancy-isolation | designed |
| M-062 | 11.5 | An explain mode MUST return (for a given query): the parsed query and any expansion, filters in effect and their sources, each candidate chunk with raw and reranked scores, excluded chunks and which filter excluded them, retrieval strategy used, and full provenance for every candidate. | `contracts/retrieval-response.md §ExplainBlock`; `contracts/retrieval-response.md §ExplainCandidate`; `contracts/retrieval-response.md §ExplainExclusion` | 4 | integration | designed |
| M-063 | 11.5 | Explain output MUST respect the same tenancy and permission rules as ordinary retrieval; it is not a bypass. | `contracts/retrieval-response.md §ExplainBlock` ("It respects the same tenancy and permission rules"); `architecture/overview.md §Tenant isolation mechanism` | 4 | tenancy-isolation | designed |
| M-064 | 12 | Contract: every stage MUST reject malformed input rather than degrading (from §18.2 contract layer, anchored by §12 invariants). | `contracts/README.md §Contract versioning` (`ContractVersionError`; "A consumer MUST reject a version it does not declare support for") | 0 | contract | designed |
| M-065 | 14.1 | Retrieval responses MUST mark content as retrieved untrusted material (chunks are labelled, not sanitized). | `contracts/retrieval-response.md §RetrievalResult` (`trust_level: enum{untrusted_ingested}`, required); `contracts/README.md §Provenance block` (`trust_level`) | 1 | security | designed |
| M-066 | 14.1 | Invisible-content (white-on-white text, zero-size fonts, off-page positioning, metadata-only text) MUST be detected and flagged at parse time. | `contracts/parse-result.md §InvisibleContentDetection`; `contracts/parse-result.md §PageResult` (`invisible_content`) | 2 | security | designed |
| M-067 | 14.1 | Ingestion-time model call prompts MUST treat document content as data, never as instruction; outputs MUST be schema-validated rather than free-form and trusted. | `architecture/provider-abstraction.md §4.4 Content-as-data enforcement` | 3 | security | designed |
| M-068 | 14.1 | Documentation MUST state plainly that the platform cannot guarantee safety of downstream generation and that the caller owns that boundary. | `contracts/retrieval-response.md §Invariants` ("Trust label always surfaced"); `contracts/README.md §Provenance block` (`trust_level`) | 7 | docs review | designed |
| M-069 | 14.2 | Connector credentials, API keys, and model provider keys MUST be stored encrypted at rest. | `architecture/provider-abstraction.md §6.1 Storage` (AES-256-GCM) | 4 | security | designed |
| M-070 | 14.2 | Credentials MUST NOT appear in logs, traces, config exports, or error messages. | `architecture/provider-abstraction.md §6.2 Log and trace exclusion`; `architecture/provider-abstraction.md §6.3 Config exports` | 1 | security | designed |
| M-071 | 14.2 | Ingestion config exports MUST be secret-free by construction so they can be committed to version control. | `contracts/ingestion-config.md §Invariants` ("Secret-free by construction"); `contracts/ingestion-config.md` (`secret_free_attestation: bool const true`) | 3 | security | designed |
| M-072 | 14.3 | When a connector ingests from a system with its own access controls, the platform MUST record source-side permissions and either mirror them into filterable fields or refuse the connection. | `contracts/inventory.md §SourcePermissions`; `contracts/README.md §Tenancy block` (`permission_mode: source_mirrored`) | 4 | tenancy-isolation | open (D-16) |
| M-073 | 14.3 | Where a connector cannot supply reliable permission data, the platform MUST say so and require explicit acknowledgement before ingestion. | `contracts/inventory.md §SourceRun` (`acknowledged_permission_gap`); `contracts/README.md §Tenancy block` (`permission_fidelity: unavailable`) | 4 | tenancy-isolation | open (D-16) |
| M-074 | 14.4 | PII/sensitive-content detection MUST NOT redact by default, because silent redaction violates the provenance invariant. | `contracts/README.md §Provenance block` (`sensitivity_flags`: "Advisory; never triggers silent redaction"); `contracts/parse-result.md §Finding` | 2 | security | designed |
| M-075 | 15 (row 1) | Embedding provider unavailable mid-ingestion: MUST pause and retry with backoff; job resumable; no partial shadow collection promoted. | `architecture/index-lifecycle.md §6` (row: embedding provider unavailable mid-ingestion); `architecture/index-lifecycle.md §3 State machine` (`INGESTING → PAUSED`) | 1 | failure-injection | designed |
| M-076 | 15 (row 2) | Embedding provider unavailable at query time: MUST fail closed with a clear error; MUST NOT silently fall back to a different model. | `architecture/index-lifecycle.md §6` (row: embedding provider unavailable at query time); `architecture/provider-abstraction.md §3.3` | 1 | failure-injection | designed |
| M-077 | 15 (row 3) | Vector DB unavailable: retrieval MUST fail closed with a distinguishable error code; ingestion pauses. | `contracts/retrieval-response.md §ErrorEnvelope` (`VECTOR_DB_UNAVAILABLE`); `architecture/index-lifecycle.md §6` | 1 | failure-injection | designed |
| M-078 | 15 (row 4) | Single document fails to parse: MUST be recorded, reported; ingestion continues. | `architecture/index-lifecycle.md §6` (single document fails to parse); `contracts/parse-result.md §Invariants` (failures represented not dropped) | 1 | failure-injection | designed |
| M-079 | 15 (row 5) | Whole content class fails: ingestion MUST halt and alert; class-wide failure is treated as a config problem. | `architecture/index-lifecycle.md §6` (whole content class fails: `INGESTING → FAILED_INGESTION` elevated) | 1 | failure-injection | designed |
| M-080 | 15 (row 6) | Shadow collection fails validation: promotion MUST be blocked, alias unchanged, alert raised, shadow retained for inspection. | `architecture/index-lifecycle.md §5 Pre-promotion validation gates`; `architecture/index-lifecycle.md §3 State machine` (`VALIDATION_FAILED` state) | 1 | failure-injection | designed |
| M-081 | 15 (row 7) | Alias swap fails mid-operation: previous alias target MUST remain authoritative; operation MUST be idempotent and retryable. | `architecture/index-lifecycle.md §4.3 Failure handling`; `architecture/index-lifecycle.md §3 State machine` (`PROMOTE_FAILED → PROMOTING`) | 1 | failure-injection | designed |
| M-082 | 15 (row 8) | Ingestion worker dies: job MUST resume from last checkpoint without duplication or loss. | `architecture/index-lifecycle.md §6` (ingestion worker dies); `architecture/index-lifecycle.md §3 State machine` (`FAILED_INGESTION → INGESTING`) | 1 | resumability | designed |
| M-083 | 15 (row 9) | Rate limit hit at provider: MUST backoff and surface remaining budget; MUST NOT fail the job. | `architecture/index-lifecycle.md §6` (rate limit hit at provider); `architecture/provider-abstraction.md §2.3 Provider surface area` | 1 | failure-injection | designed |
| M-084 | 15 (row 10) | Query returns zero results: MUST distinguish "no matches" from "filtered to nothing" from "error" in the response. | `contracts/retrieval-response.md §result_status distinction` (`enum{matches, no_matches, filtered_to_zero, error}`) | 1 | contract | designed |
| M-085 | 16 | Scheduled operations MUST respect budget caps and MUST alert on repeated cap-hits rather than quietly stopping. | `configuration/reference.md §2.9` (`budgets.scheduled_reindex_cap_hit_alert_count`); `architecture/index-lifecycle.md §7.3` | 4 | unit | designed |
| M-086 | 17.1 | When a document is deleted, every derived artifact MUST be removed: segments, chunks, embeddings (live and N-1), augmentation fields, and eval questions. | `architecture/index-lifecycle.md §12.1 What "deleted" means` (steps 1–6); `contracts/eval-set.md §Invariants` (deletion linkage) | 4 | deletion | designed |
| M-087 | 17.1 | Restoring a snapshot MUST replay the tombstone log before the restored collection is eligible for promotion. | `architecture/index-lifecycle.md §10.4 Cold restore` (step 3: TOMBSTONE_REPLAY); `architecture/index-lifecycle.md §1 Invariants` (invariant 6) | 4 | deletion | designed |
| M-088 | 17.1 | Snapshot retention periods MUST be documented, since they bound how long deleted content can persist. | `configuration/reference.md §2.6` (`index_lifecycle.snapshot_retention_period_days`, pending D-05); `runbooks/README.md` (`backup.md`) | 4 | docs review | open (D-05) |
| M-089 | 17.1 | The distinction between "deleted from service" and "purged from all copies" MUST be visible in the UI. | `architecture/index-lifecycle.md §12.2 Purge vs delete` (table: delete vs purge); `runbooks/README.md` (`purge.md`) | 6 | integration | designed |
| M-090 | 17.3 | A knowledge base MUST be fully exportable: ingestion config, class descriptions, eval sets, findings reports, and optionally chunks with provenance. | `contracts/ingestion-config.md §Invariants` ("Diffable/re-importable"); `architecture/provider-abstraction.md §6.3 Config exports` | 4 | integration | designed |
| M-091 | 18.1 | A version-controlled golden corpus fixture MUST be built in Phase 0, containing the specified document types including an adversarial document. | `architecture/segment-taxonomy.md §5.2 Golden corpus fixture coverage` | 0 | unit | designed |
| M-092 | 18.1 | Fixtures MUST be synthetic or licence-clear; real client documents MUST NOT be seeded in the repository. | `architecture/segment-taxonomy.md §5.2` (fixture descriptions are all synthetic/described); `architecture/overview.md` | 0 | docs review | designed |
| M-093 | 18.2 | Contract test layer: each contract in Section 12 MUST be validated at every producer/consumer boundary; a stage MUST reject malformed input rather than degrading. | `contracts/README.md §Contract versioning` (`ContractVersionError`; "A consumer MUST reject a version it does not declare support for, rather than parse it best-effort") | 0 | contract | designed |
| M-094 | 18.2 | Tenancy isolation test layer: cross-tenant retrieval attempts MUST fail; break-glass MUST produce audit records; negative tests are mandatory. | `architecture/overview.md §Tenant isolation mechanism` (Test §18.3 test 2); `contracts/README.md §Audit & break-glass seeds` | 0 | tenancy-isolation | designed |
| T-01 | 18.3 (test 1) | Alias swap under sustained query load — zero errors. | `architecture/index-lifecycle.md §11 Zero-downtime acceptance tests (Test 1)` | 1 | 18.3 #1 | designed |
| T-02 | 18.3 (test 2) | Cross-tenant retrieval attempt — fails closed. | `architecture/overview.md §Tenant isolation mechanism` (Test §18.3 test 2 described in full) | 4 | 18.3 #2 | designed |
| T-03 | 18.3 (test 3) | Provenance completeness — every chunk from the golden corpus, no exceptions. | `contracts/chunk.md §Invariants` ("Provenance complete for every chunk — non-null `Provenance`…Non-negotiable test §18.3.3") | 1 | 18.3 #3 | designed |
| T-04 | 18.3 (test 4) | Tier 2 byte-identity — augmented chunks return verbatim source text. | `contracts/chunk.md §Invariants` ("Tier 2 never touches `text` — every Tier 2 op has `changed_text=false`…This is exactly what §18.3 test 4 asserts"); byte-identity defined against Tier-1-normalized canonical source (D-11 CLOSED 2026-09-03) | 3 | 18.3 #4 | designed |
| T-05 | 18.3 (test 5) | Break-glass audit — every content read under a grant produces a record. | `contracts/README.md §Audit & break-glass seeds` (`AuditRecord.grant_id`); `runbooks/README.md` (`break-glass.md`) | 4 | 18.3 #5 | designed |
| T-06 | 18.3 (test 6) | Rollback — alias reverts to N-1 and serves correctly. | `architecture/index-lifecycle.md §11 Zero-downtime acceptance tests (Test 6)`; `architecture/index-lifecycle.md §10.3 Instant rollback` | 4 | 18.3 #6 | designed |
| T-07 | 18.3 (test 7) | Embedding model mismatch — a query against an index built with a different model fails closed rather than returning results. | `architecture/provider-abstraction.md §3 Model identity pinning`; `architecture/provider-abstraction.md §3.3 The mismatch check` | 1 | 18.3 #7 | designed |
| T-08 | 18.3 (test 8) | Deletion completeness — deleted content absent from live, N-1, and post-restore collections. | `architecture/index-lifecycle.md §12.2 Purge vs delete`; `architecture/index-lifecycle.md §12.1` | 4 | 18.3 #8 | designed |
| T-09 | 18.3 (test 9) | Injection flagging — the adversarial fixture is flagged and its suspicion score is retrievable. | `contracts/README.md §Provenance block` (`injection_suspicion`); `contracts/chunk.md §Golden-corpus expressibility` (adversarial doc section) | 2 | 18.3 #9 | designed |
| T-10 | 18.3 (test 10) | Secret hygiene — no credential appears in any log, trace, error, or config export. | `architecture/provider-abstraction.md §6.2 Log and trace exclusion`; `architecture/provider-abstraction.md §6.3 Config exports` | 1 | 18.3 #10 | designed |
| M-095 | 19 (Phase 0) | Tenancy fields MUST be present in every contract from Phase 0, even though enforcement arrives in Phase 4. | `contracts/README.md §Tenancy block` ("Present in all six contracts from Phase 0"); `architecture/overview.md §Tenancy from day zero` | 0 | contract | designed |
| M-096 | 10.2 | UI MUST display the memory cost when the hot-copy retention count is raised. | `architecture/index-lifecycle.md §10.2 Memory cost display` | 6 | integration | designed |
| M-097 | 6.4 (prose) | Prose: structure-aware chunking respecting heading boundaries; parent breadcrumb augmentation default-on for documents with real heading structure. | `architecture/segment-taxonomy.md §2.1` (prose row: "Structure-aware chunking at heading boundaries. Parent breadcrumb augmentation default-on"); `contracts/ingestion-config.md §ChunkingStrategy` (`respect_headings`) | 2 | unit | designed |
| M-098 | 6.4 (scans) | Scans: per-page OCR confidence in the payload; low-confidence segments down-weighted at retrieval and flagged in provenance. | `contracts/README.md §Provenance block` (`ocr_confidence`); `architecture/segment-taxonomy.md §2.1` (scanned_region row); `architecture/segment-taxonomy.md §4.1` (OCR confidence signals) | 2 | retrieval-quality | designed |
| M-099 | 6.4 (code) | Code: syntax-aware splitting at function/class boundaries. | `architecture/segment-taxonomy.md §2.1` (code row: "Syntax-aware splitting at function/class/declaration boundaries"); `contracts/ingestion-config.md §ChunkingStrategy` (`split_boundaries`) | 2 | unit | designed |
| M-100 | 9.4 | Sweep baseline score is retained, re-run on schedule and after every reindex; regression against the retained baseline raises an alert. | `configuration/reference.md §2.10` (`observability.drift_detection_cron`); `architecture/index-lifecycle.md §5 Gate 4` | 5 | retrieval-quality | designed |
| M-101 | 11.3 | Per-tenant rate limits and quotas enforced at the retrieval service. | `configuration/reference.md §2.8` (`retrieval.rate_limit.queries_per_second_per_tenant`); `architecture/overview.md §Services` (retrieval-api) | 4 | tenancy-isolation | designed |
| M-102 | 13.2 | Required telemetry implemented across all five domains: ingestion, index, retrieval, quality, governance. | `architecture/overview.md §Core library decomposition` (`observability/` package with OTel + Prometheus); `configuration/reference.md §2.10 Observability` | 4 | integration | designed |
| M-103 | 4.6 | One-command bring-up: `docker compose up` reaches a working system; first run prompts for a model provider and validates it. | `architecture/provider-abstraction.md §7.3 First-run prompt`; `configuration/reference.md §2.1` (`platform.first_run_complete`) | 0 | integration | designed |
| M-104 | 4.6 | Config as one file: single declarative config, every option commented with defaults; env vars override; nothing hidden in code. | `configuration/reference.md §1 Config model` ("one declarative file…every option is present in the file, commented, with its default shown. Nothing is hidden in code") | 0 | unit | designed |
| M-105 | 14.1 | Injection suspicion score is retrievable and filterable, and is NOT used to silently exclude content. | `contracts/README.md §Provenance block` (`injection_suspicion`: "Retrievable and filterable; never used to silently exclude"); `contracts/segment-set.md §Segment` (`injection_suspicion`) | 2 | security | designed |

> Rows M-096..M-105 were added by the orchestrator after reviewing the extraction pass:
> the §6.4 handling matrix is prefaced "specified, not left to the executor" and is therefore
> binding despite not using the word MUST; likewise §4.6 ("Requirements:"), §9.4, §11.3,
> §13.2 ("Required telemetry"). IDs are append-ordered, not spec-ordered.
