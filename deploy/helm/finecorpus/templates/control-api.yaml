{{- /* Maps to compose service: control-api (SERVICE=control-api, uvicorn :8000) */ -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "finecorpus.fullname" . }}-control-api
  labels:
    {{- include "finecorpus.labels" . | nindent 4 }}
    app.kubernetes.io/component: control-api
spec:
  type: {{ .Values.controlApi.service.type }}
  ports:
    - name: http
      port: {{ .Values.controlApi.service.port }}
      targetPort: {{ .Values.controlApi.service.targetPort }}
  selector:
    {{- include "finecorpus.labels" . | nindent 4 }}
    app.kubernetes.io/component: control-api
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "finecorpus.fullname" . }}-control-api
  labels:
    {{- include "finecorpus.labels" . | nindent 4 }}
    app.kubernetes.io/component: control-api
spec:
  replicas: {{ .Values.controlApi.replicaCount }}
  selector:
    matchLabels:
      {{- include "finecorpus.labels" . | nindent 6 }}
      app.kubernetes.io/component: control-api
  template:
    metadata:
      labels:
        {{- include "finecorpus.labels" . | nindent 8 }}
        app.kubernetes.io/component: control-api
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
    spec:
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- include "finecorpus.imagePullSecrets" . | nindent 6 }}
      {{- with .Values.nodeSelector }}
      nodeSelector: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity: {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: control-api
          image: {{ include "finecorpus.image" (dict "img" .Values.app.image "global" .Values.global) }}
          imagePullPolicy: {{ .Values.app.image.pullPolicy }}
          env:
            - name: SERVICE
              value: control-api
            {{- include "finecorpus.appEnv" . | nindent 12 }}
          ports:
            - name: http
              containerPort: 8000
          {{- include "finecorpus.httpProbes" . | nindent 10 }}
          volumeMounts:
            - name: config
              mountPath: /app/config
              readOnly: true
          resources:
            {{- toYaml .Values.controlApi.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "finecorpus.fullname" . }}-config
